import { useState } from 'react';
import CreatePostForm from '../CreatePostForm';

function Compose() {
    return (
        <div>
            <h1>Compose</h1>
            <CreatePostForm />
        </div>
    );
}

export default Compose;
